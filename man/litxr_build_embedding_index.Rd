% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sync.R
\name{litxr_build_embedding_index}
\alias{litxr_build_embedding_index}
\title{Build a cached embedding index for one collection field}
\usage{
litxr_build_embedding_index(
  collection_id,
  config = NULL,
  field = "abstract",
  embed_fun,
  model,
  provider = NA_character_,
  batch_size = 64L,
  overwrite = FALSE,
  limit = NULL
)
}
\arguments{
\item{collection_id}{Collection identifier from \code{config.yaml}.}

\item{config}{Optional parsed config list or a direct config path. When
omitted, \code{litxr} reads \code{LITXR_CONFIG}.}

\item{field}{Text field to embed, such as \code{"abstract"}.}

\item{embed_fun}{Function taking a character vector and returning embeddings
as a matrix, data frame, or list of numeric vectors.}

\item{model}{Exact embedding model name. The same value must be used for
search queries.}

\item{provider}{Optional provider label, such as \code{"openrouter"} or
\code{"gemini"}.}

\item{batch_size}{Number of texts per embedding request.}

\item{overwrite}{Whether to rebuild the cache from scratch.}

\item{limit}{Optional maximum number of new texts to embed in this run.}
}
\value{
Metadata \code{data.table} for cached embeddings.
}
\description{
Embeds one text field from \code{litxr_read_collection()} in batches and caches the
resulting numeric matrix under \verb{project.data_root/embeddings/}. The embedding
function is user supplied so callers can use providers such as
\code{inferencer::embed_openrouter()} or \code{inferencer::embed_gemini()}.
}
