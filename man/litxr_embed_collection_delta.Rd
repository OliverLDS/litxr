% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sync.R
\name{litxr_embed_collection_delta}
\title{Build pending embedding delta shards for one collection field}
\usage{
litxr_embed_collection_delta(
  collection_id,
  config = NULL,
  field = "abstract",
  embed_fun,
  model,
  provider = NA_character_,
  batch_size = 64L,
  overwrite = FALSE,
  limit = NULL
)
}
\arguments{
\item{collection_id}{Collection identifier from \code{config.yaml}.}

\item{config}{Optional parsed config list or a direct config path. When
omitted, \code{litxr} reads \code{LITXR_CONFIG}.}

\item{field}{Text field to embed, such as \code{"abstract"}.}

\item{embed_fun}{Function taking a character vector and returning embeddings
as a matrix, data frame, or list of numeric vectors.}

\item{model}{Exact embedding model name. The same value must be used for
search queries.}

\item{provider}{Optional provider label, such as \code{"openrouter"} or
\code{"gemini"}.}

\item{batch_size}{Number of texts per embedding request.}

\item{overwrite}{Whether to rebuild the delta target set from scratch.}

\item{limit}{Optional maximum number of new texts to embed in this run.}
}
\value{
Metadata \code{data.table} for the merged main-plus-delta embedding state.
}
\description{
Embeds only records that are not already present in the main cache or delta
shards, then appends those batches to the delta layer without compacting the
main cache.
}
