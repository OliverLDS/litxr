% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sync.R
\name{litxr_label_collection_by_category}
\alias{litxr_label_collection_by_category}
\title{Label collection records from aggregated category scores}
\usage{
litxr_label_collection_by_category(
  category_scores,
  score_col = "score_max",
  threshold = 0,
  multi_label = FALSE,
  margin = NULL
)
}
\arguments{
\item{category_scores}{Aggregated score table from
\code{litxr_score_collection_categories()}.}

\item{score_col}{Score column used for labeling, such as \code{"score_max"} or
\code{"score_mean"}.}

\item{threshold}{Minimum score required for assignment. May be a scalar or a
named numeric vector keyed by \code{category_id}.}

\item{multi_label}{Whether to return all categories above threshold for each
record instead of a single best category.}

\item{margin}{Optional minimum gap between the top-ranked and second-ranked
categories in single-label mode.}
}
\value{
In single-label mode, one row per \code{ref_id}. In multi-label mode, one
row per assigned \code{ref_id} and \code{category_id}.
}
\description{
Applies simple threshold rules to aggregated category scores produced by
\code{litxr_score_collection_categories()}.
}
