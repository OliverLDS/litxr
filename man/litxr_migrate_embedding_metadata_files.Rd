% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/embedding_state.R
\name{litxr_migrate_embedding_metadata_files}
\alias{litxr_migrate_embedding_metadata_files}
\title{Rewrite legacy embedding metadata files to the narrow two-column shape}
\usage{
litxr_migrate_embedding_metadata_files(
  collection_id,
  config = NULL,
  field = "abstract",
  model,
  recurse = TRUE
)
}
\arguments{
\item{collection_id}{Collection identifier from \code{config.yaml}.}

\item{config}{Optional parsed config list or a direct config path. When
omitted, \code{litxr} reads \code{LITXR_DATA_ROOT}.}

\item{field}{Embedded text field.}

\item{model}{Exact embedding model name used when building the cache.}

\item{recurse}{Whether to scan recursively for \code{metadata.fst} files.}
}
\value{
Character vector of rewritten paths.
}
\description{
This helper rewrites any \code{metadata.fst} file found under one embedding cache
directory so that persisted metadata only contains \code{ref_id} and \code{abstract}.
The \code{abstract} values are rehydrated from the collection JSON files using the
shard \code{ref_id} values, and existing extra columns are dropped.
}
