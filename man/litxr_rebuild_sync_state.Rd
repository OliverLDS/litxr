% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sync.R
\name{litxr_rebuild_sync_state}
\alias{litxr_rebuild_sync_state}
\title{Rebuild a first-pass sync ledger from existing local data}
\usage{
litxr_rebuild_sync_state(config = NULL, overwrite = FALSE)
}
\arguments{
\item{config}{Optional parsed config list or a direct config path. When
omitted, \code{litxr} reads \code{LITXR_CONFIG}.}

\item{overwrite}{Whether to replace the existing sync ledger instead of only
adding inferred rows for collections not already present.}
}
\value{
\code{data.table} of the rebuilt sync ledger.
}
\description{
Infers one sync ledger row per collection from existing local storage and
index file timestamps. This is intended for backfilling \code{sync_state.fst} when
local data already exists from older package versions that did not record
sync history.
}
\details{
The inferred rows are approximate. They record that local data exists and
when the local collection index was last modified, but they do not recover
exact remote cursors, arXiv day windows, or original sync arguments.
}
