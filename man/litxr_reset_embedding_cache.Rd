% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sync.R
\name{litxr_reset_embedding_cache}
\alias{litxr_reset_embedding_cache}
\title{Reset one embedding cache while optionally preserving delta shards}
\usage{
litxr_reset_embedding_cache(
  collection_id,
  config = NULL,
  field = "abstract",
  model,
  preserve_delta = TRUE
)
}
\arguments{
\item{collection_id}{Collection identifier from \code{config.yaml}.}

\item{config}{Optional parsed config list or a direct config path. When
omitted, \code{litxr} reads \code{LITXR_CONFIG}.}

\item{field}{Embedded text field.}

\item{model}{Exact embedding model name used when building the cache.}

\item{preserve_delta}{Whether to keep existing delta shards.}
}
\value{
List with \code{before}, \code{after}, and \code{removed}.
}
\description{
Removes the main on-disk embedding cache for one collection field and model.
By default, pending delta shards are preserved so any successfully embedded
batches remain available for later recovery or compaction. This is useful
when the main cache is stale or corrupted, such as a zero-byte legacy
\code{matrix.rds}.
}
