% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sync.R
\name{litxr_score_collection_categories}
\alias{litxr_score_collection_categories}
\title{Score one collection against category-query embeddings}
\usage{
litxr_score_collection_categories(
  collection_id,
  query_set_id = "default",
  config = NULL,
  field = "abstract",
  model,
  aggregations = c("max", "mean"),
  top_k = 3L,
  ref_ids = NULL,
  include_query_scores = FALSE
)
}
\arguments{
\item{collection_id}{Collection identifier from \code{config.yaml}.}

\item{query_set_id}{Identifier of a cached category query set.}

\item{config}{Optional parsed config list or a direct config path. When
omitted, \code{litxr} reads \code{LITXR_CONFIG}.}

\item{field}{Embedded text field. Must match the collection embedding index.}

\item{model}{Exact embedding model name. Must match both corpus and query
indexes.}

\item{aggregations}{Character vector of aggregation names chosen from
\code{"max"}, \code{"mean"}, and \code{"top_k_mean"}.}

\item{top_k}{Number of top query scores to average when
\code{aggregations} includes \code{"top_k_mean"}.}

\item{ref_ids}{Optional subset of collection \code{ref_id}s to score.}

\item{include_query_scores}{Whether to include the full per-query score table.}
}
\value{
If \code{include_query_scores = FALSE}, a \code{data.table} with one row per
\code{ref_id} and \code{category_id}. Otherwise a list with \code{category_scores} and
\code{query_scores}.
}
\description{
Scores each collection record against all query sentences in a cached category
query index, then aggregates within category by \code{max}, \code{mean}, or
\code{top_k_mean}.
}
