% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sync.R
\name{litxr_score_collection_categories_delta}
\alias{litxr_score_collection_categories_delta}
\title{Score cached category queries against embedding delta shards}
\usage{
litxr_score_collection_categories_delta(
  collection_id,
  query_set_id = "default",
  config = NULL,
  field = "abstract",
  model,
  aggregations = c("max", "mean"),
  top_k = 3L,
  ref_ids = NULL,
  date = NULL,
  shard = NULL,
  include_query_scores = FALSE
)
}
\arguments{
\item{collection_id}{Collection identifier from \code{config.yaml}.}

\item{query_set_id}{Identifier of a cached category query set.}

\item{config}{Optional parsed config list or a direct config path. When
omitted, \code{litxr} reads \code{LITXR_CONFIG}.}

\item{field}{Embedded text field. Must match the delta shard model cache.}

\item{model}{Exact embedding model name used for both the collection delta
shards and the cached query set.}

\item{aggregations}{Character vector of aggregation rules to compute. Choose
from \code{"max"}, \code{"mean"}, and \code{"top_k_mean"}.}

\item{top_k}{Positive integer used when \code{aggregations} includes
\code{"top_k_mean"}.}

\item{ref_ids}{Optional subset of \code{ref_id} values to score.}

\item{date}{Optional date used to filter delta shard filenames. Accepts
values coercible by \code{as.Date()}.}

\item{shard}{Optional shard filename or full shard path under the collection's
embedding delta directory.}

\item{include_query_scores}{Whether to also return per-query scores before
aggregation.}
}
\value{
Aggregated category-score \code{data.table}, or a list with
\code{category_scores} and \code{query_scores} when \code{include_query_scores = TRUE}.
}
\description{
Scores a cached category-query index against one or more selected embedding
delta shards for the same collection field and model. This uses locally
cached query vectors only and does not call any external embedding API.
}
