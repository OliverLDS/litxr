% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sync.R
\name{litxr_search_embedding_delta}
\alias{litxr_search_embedding_delta}
\title{Search embedding delta shards instead of the compacted main cache}
\usage{
litxr_search_embedding_delta(
  query,
  collection_id,
  config = NULL,
  field = "abstract",
  embed_fun = NULL,
  query_vec = NULL,
  model,
  date = NULL,
  shard = NULL,
  top_n = 20L
)
}
\arguments{
\item{query}{Query text. Ignored when \code{query_vec} is supplied.}

\item{collection_id}{Collection identifier from \code{config.yaml}.}

\item{config}{Optional parsed config list or a direct config path. When
omitted, \code{litxr} reads \code{LITXR_CONFIG}.}

\item{field}{Embedded text field.}

\item{embed_fun}{Function taking a character vector and returning embeddings.
Required only when \code{query_vec} is not supplied.}

\item{query_vec}{Optional precomputed numeric query embedding vector.}

\item{model}{Exact embedding model name. Must match the selected delta shard
model.}

\item{date}{Optional date used to filter shard filenames. Accepts values
coercible by \code{as.Date()}.}

\item{shard}{Optional shard filename or full shard path under the collection's
embedding delta directory.}

\item{top_n}{Number of matches to return.}
}
\value{
\code{data.table} of matches with \code{score}.
}
\description{
Embeds the query with the supplied \code{embed_fun}, or uses a precomputed
\code{query_vec}, then ranks records from one or more selected delta shard files
by cosine similarity. Use \code{date} to select shard files whose timestamp prefix
matches a \code{YYYY-MM-DD} day, or \code{shard} to point at one specific shard file
directly.
}
